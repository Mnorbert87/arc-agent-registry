[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# Arc testnet RPC for `forge script --broadcast` and `forge create`.
[rpc_endpoints]
arc_testnet = "https://rpc.testnet.arc.network"
